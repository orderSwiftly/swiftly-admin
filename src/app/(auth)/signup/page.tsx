import SignupComp from "./sign-up"

export default function SignUp() {
    return (
        <main className="flex min-h-screen items-center justify-center bg-[var(--bg-clr)]">
            <SignupComp />
        </main>
    )
}